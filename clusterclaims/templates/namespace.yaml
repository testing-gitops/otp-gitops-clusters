apiVersion: v1
kind: Namespace
metadata:
  annotations:
    openshift.io/display-name: {{ .Values.clusterClaimName }}
    cluster.open-cluster-management.io/managedCluster: {{ .Values.clusterClaimName }}
    kubernetes.io/metadata.name: {{ .Values.clusterClaimName }}
    argocd.argoproj.io/sync-wave: "340"
    helm.sh/hook-weight: "340"
  name: {{ .Values.clusterClaimName }}